<h1>チャットルーム</h1>

<% @messages.each do |message| %>
<p><%= message.name %> >> <%= message.content %>  [<%= message.time.in_time_zone('Tokyo').to_s(:time) %>]</p>
<% end %>

<p><%= link_to "メッセージ作成", new_room_path %></p>
